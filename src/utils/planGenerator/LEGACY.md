# LEGACY

This folder powers the unrouted plan-generator (`src/pages/Index.tsx`).
It is not the public Predictiv Finder matching path.

Public triage: `supabase/functions/analyze-health-concern` + `professionals`.
Do not import this module from `src/pages/site/` or new public routes.
